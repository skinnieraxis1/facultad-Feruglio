## Repaso

### Cuales son los puntos comunes y no comunes entre los LN y los LF

Los lenguajes Formales evolucionan de forma discreta mientras que los naturales de forma continua

### Como se pueden definir los LF

- Caracteres => 2 ; 1 ; = ; a



- Palabras => 2 ; 123 ; 2 = 1 + 1 ; Yo llego ahora.

Unidad de traducción: El mínimo necesario para generar una palabra. 

Secuencia de caracteres con o sin errores sintácticos 
- Token => Yo ; Llego ; Ahora ; . 


### Cuales son los elementos de Σ* (Lenguaje Universal)
LU -> Σ* <br>
|Σ*| = Infinito <br>
Σ = {0, 1} => Σ* = {Σ, 0, 1, 00, 01, 10, 11, ...}

p(Σ) = {{}, {0}, {1}, {0,1}}

|p(Σ)| = 4 = 2^|Σ|

|Σ| = 2

### Que significa P(Σ*) = 2^Σ*

Σ = ASCII

Σ = Unicode

Unicode* => P(Unicode*) = Todos los posibles lenguajes que puedo armar con Unicode

### Para que sirven los módulos y que partes tienen

Modularizar es el crear módulos para separar "Funciones"

### Relación entre proveedor, consumidor e interfaz

El proveedor es el que le dá un servicio al consumidor que lo va a utilizar, la interfaz es el intermediario que organiza que consumidor requiere que proveedor el llamado . <br>
Como en el POO que existe interface o en C tenemos los #include

### Que pragmática tiene #include<interfaz.h>

wa

